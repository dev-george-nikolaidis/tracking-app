import { StyleSheet, Text, View } from "react-native";
interface Props {}

const ManageExpense: React.FC<Props> = ({}) => {
	return (
		<View>
			<Text>Manage Expenses</Text>
		</View>
	);
};

export default ManageExpense;

const styles = StyleSheet.create({});
